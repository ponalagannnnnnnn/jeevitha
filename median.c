#include<stdio.h>
void main()
{
int i,n,temp,arr[100];
printf("\n enter the size");
scanf("%d",&size);
printf("'\n enter the array no");
for(i=0;i<n;i++)
{
scanf("%d",&array);
}
for(i=0;i<n;i++)
{
for(j=i+1;j<n;j++)
{
if(a[i]>aj])
{
temp=a[i];
a[i]=a[j];
a[j]=temp;
}
}
}
printf("\n the median element is %d",&a[n/2]);
getch();
}
