#include<stdio.h>
void main()
{
int n,a,b;
printf("\n enter the value\n");
scanf("%d",&n&a&b);
a=('a'to'z');
b=('A'TO 'Z');
if(a||b)
{
printf("\n the value is alphabet);
else
printf("\n the value not alphabet);
}
getch();
}
